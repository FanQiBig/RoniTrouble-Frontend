# 从 MineSidebar.vue 中移除 getProfileStats 调用

## 修改文件
`components/MineSidebar.vue`

## 具体改动

### 1. 删除导入中的 getProfileStats
- 从第47行的 import 语句中删除 `getProfileStats`

### 2. 删除 refreshStats 函数
- 删除或注释掉第99-109行的 `refreshStats` 函数

### 3. 移除 StatsCard 组件
- 删除模板中的 `<StatsCard :stats="stats" @tap="handleStatsTap" />`
- 删除 import 中的 `StatsCard`
- 删除 stats reactive 对象
- 删除 handleStatsTap 函数